[build]
  base = "app_design"
  command = "cd .. && npm install && cd app_design && npm install && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"